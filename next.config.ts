// Next.js Config

const nextConfig = {
  /* config options here */

  typescript: {
    // ignoreBuildErrors: false,
  },
  images: {
    domains: ['api.dicebear.com'],
  },
};

export default nextConfig;
